## v0.3.0 (current)

- Add global app versioning via `VERSION` file.
- Expose `GET /version` endpoint returning `{ version }`.
- Show version in UI header (list + detail views).
- Many earlier fixes: template errors, Ajax status/anrede updates, copy buttons, versioning of leads, PDF import enhancements.

